# KCIASSO — PROJECT_STATE

Last updated: 2026-07-21, Stage I8.4 acceptance closure completed.

## Logical project

- Frontend: `D:\Desktop\dev\web\orders\kciasso-frontend`
- Backend: `D:\Desktop\dev\web\orders\kciasso-backend`
- Canonical report: `D:\Desktop\dev\web\orders\kciasso-backend\отчёт.txt`

## Stack and architecture

- Frontend: Next.js 16, React 19, TypeScript, App Router, SCSS Modules, Kubb.
- Backend: NestJS 10, Prisma 5, PostgreSQL, Swagger/OpenAPI and JWT sessions.
- Administrative access is stored directly on `User`: `role`, `is_active`, site/news capability flags, `documents_access_mode` and fixed `document_groups`.
- `UserSectionPermission` remains in the database only for migration compatibility and is not a runtime authorization source.
- Documents use one Document/versions/files model with fixed registry placements and quarantine-backed storage.
- Stage I8 adds additive `PublicationStatus`, per-News/per-DocumentPlacement publication windows, `StoredFile` globally unique SHA dedup references, and DB-backed admin auth rate-limit buckets.
- Publication worker uses `@nestjs/schedule` minute/startup sweeps with PostgreSQL advisory lock; public queries remain time-window source of truth.
- News supports an external image URL or an owned JPG/PNG/WebP file up to 10 MB. Owned media is stored under `NEWS_MEDIA_ROOT` (`/app/storage/media/news` in containers) and exposed through an opaque storage key.
- News categories append an omitted order as `max(order)+1` under a PostgreSQL advisory transaction lock; existing zero-order rows are preserved. News/category create slugs are optional and generated by the shared Cyrillic transliteration utility with collision suffixes.
- News cover state uses external URL or owned JPG/PNG/WebP media as one persisted `cover_image_url`; frontend has explicit upload/url/remove state, keyed controlled inputs and blob preview cleanup. Null update clears the persisted cover and owned files are deleted only after successful mutation when unreferenced.
- Documents admin page has an explicit resolver for default, all, group and exact-placement contexts. Unknown or forbidden query keys fall back to the neutral page context and never become visible labels.
- Document share links are document-scoped: creation takes a PostgreSQL transaction advisory lock, revokes all prior non-revoked links for the document, and inserts one new version-pinned link. Admin list returns only the current non-revoked link; raw token is returned only by create.

## Main authorization decisions

- Roles are `SUPER_ADMIN` and `ADMIN`; only `SUPER_ADMIN` can manage administrators.
- Public registration is removed. Administrator creation uses the protected super-admin endpoint.
- Inactive users cannot log in and existing sessions are rejected on every protected request.
- Site settings and news are independently controlled by capability flags.
- Document modes are `NONE`, `ALL` and `SELECTED_GROUPS`; an empty selected-groups array grants no document access.
- Fixed document groups: `GIA_9`, `GIA_11`, `GIA`, `QUALITY`, `REGIONAL`, `ABOUT`.
- Mixed-scope documents are visible only in an allowed placement with `canManage: false`; management endpoints return 403, while reorder within the allowed placement remains available.
- Subadmins mutate only visible placements; global document delete/status/version replacement is SUPER_ADMIN-only. Shared StoredFile deletion is reference-aware.
- The last active `SUPER_ADMIN` is transactionally protected from deletion, deactivation and demotion.
- The “Полный доступ к контенту” preset is a frontend convenience and does not grant user management.

## Main routes and data

- Public API is under `/api/public/**`; administrative API is under `/api/admin/**`; login/session routes are under `/api/user/**`.
- News media upload/removal is under `/api/admin/news/media`; public owned media is streamed under `/api/public/news/media/:key`.
- Documents use versioned files, fixed registry placements and version-pinned share links. Main files remain in persistent `storage/documents`; temporary uploads use `storage/tmp`.
- Frontend public routes include home, news, GIA, education quality, resources and centre pages; administration is under `/admin/**`.
- Admin document placement replacement is `PUT /api/admin/documents/:id/placements`; complete-list reorder is `PATCH /api/admin/document-placements/reorder`.
- News publication command is `POST /api/admin/news/:id/publication`; placement command is `POST /api/admin/documents/:id/placements/:sectionKey/publication`.

## Commands and generated paths

- Frontend checks: `npm run test:unit`, `npm run test:browser -- --workers=1`, `npm run lint`, `npm run build`.
- Backend checks: `npx prisma generate`, `npm test -- --runInBand`, `npm run test:e2e`, `npm run build`.
- Backfill: `npm run documents:backfill-stored-files -- --dry-run|--apply [--report=<path>]`.
- Backend production start: `npm run start:container`; seed is controlled by `RUN_SEED_ON_BOOT=true`.
- Generated paths: frontend Kubb client under `src/shared/api/generated`; backend Prisma DTOs under `src/.generated/prisma`; standard Prisma runtime package under `node_modules/@prisma/client` with internal generated runtime in `.prisma/client`. Do not edit generated output manually.

## Verification

- Frontend unit tests: 12 files, 43/43 passed; lint and production build passed.
- Backend unit tests: 5 suites, 30/30 passed; E2E: 10 suites, 85/85 passed; production build passed.
- Non-fixing ESLint for all changed backend TypeScript files passed.
- Full backend ESLint still reports 19 pre-existing formatting issues in unrelated site-settings/error DTO files.
- Placement selector browser acceptance passed at 1366×768 and 390×844: wheel/programmatic scrolling, last item visibility, fixed footer, body lock restoration and no horizontal overflow.
- Backend `npx prisma generate`, `npm run build` and unit tests pass after switching Prisma Client output to the standard `.prisma/client` path. Clean `npm ci` copy also generates and builds successfully.
- Node 22.23.1 clean `npm ci`, Prisma generate, build, unit tests and local startup pass; generated DTO/compiled imports use `@prisma/client`.
- Stage I1 isolated news order/slug e2e: 3/3 passed; frontend Kubb regenerated from task backend Swagger after optional slug DTO change; frontend unit/lint/build pass.
- Stage I2 isolated news-media e2e: 3/3 passed; frontend browser mode/preview/remove acceptance passed at 390×844 and 1366×768 against isolated E2E DB/media root; Kubb regenerated for nullable cover URL.
- Stage I3 document-share e2e: 8/8 passed, including sequential replacement, cross-version replacement and concurrent create invariant. Frontend unit: 13 files, 47/47; lint/build passed. Real browser acceptance verified neutral/all/group/exact/tampered headings, one current share row, raw URL hidden after reload and revoke leaving zero active rows; no console errors after serving standalone static assets.
- Stage I4 document query pipeline implemented: access-first scope, group/placement/search/status filters, DB sorting and server pagination; frontend URL controls and safe exact-placement reorder gating. Frontend unit 49/49, lint/build pass; backend unit 32/32 and build pass. Browser/API acceptance remains pending because the user-owned backend on port 4000 could not be restarted safely.
- Stage I4.1 complete: isolated runtime on 4441 with migrations and 130+ documents, real HTTP matrix and Playwright desktop/mobile passed. Empty pagination is totalPages=0; Kubb generated 195 files from current Swagger; restricted group options and >100 exact-placement reorder gating verified. Temp DB/runtime/processes removed; user ports 3000/4000 preserved.
- Stage I5 acceptance complete on Node 22.23.1 portable runtime: migrations/startup, I1/I2/I3/I4 targeted e2e, permission API matrix, admin desktop/mobile browser and placement scroll metrics all pass. Docker daemon was unavailable, so production deploy readiness remains blocked pending CI/container build.
- Task-owned backend/frontend runtime acceptance passed on ports 4417/3417; `/api/user/me` and protected `/admin` were verified.
- Stage I5.1 complete: persisted news cover external URL → PNG → WebP → external URL → null was exercised through real browser save/reload on isolated runtime; media cleanup/failure boundaries passed targeted e2e; desktop/mobile console and overflow checks passed. Production blocker remains Docker/CI build.

## Current verification

- Stage I8: additive migration and Prisma/Kubb-compatible generation pass; StoredFile reuse, publication commands/windows, startup/minute worker, DB-backed auth buckets and explicit trust-proxy env are implemented. Backend unit 32/32 and frontend unit 55/55 pass; frontend lint/build and backend lint/build pass. Node 22.14.0 isolated runtime on 4487/3487 smoke endpoints returned 200.
- Stage I8 targeted backend E2E: news, auth, permissions, share-links, placements, site-settings, media and service cleanup pass. Remaining legacy expectations: ordinary-admin version replacement expects 201 but new SUPER_ADMIN-only policy returns 403; shared-blob deletion test expects a duplicated SHA to remain independently readable, conflicting with physical dedup semantics.
- Stage I8 Playwright command was attempted but repository config targets unavailable user ports 3000/4479; no user ports were started or touched.

- Stage I6A: empty slug is omitted/normalized, cover mutations are authoritative, upload replacement preserves the file input, and action errors preserve values. Frontend unit 51/51, backend news e2e 7/7, lint/build and real desktop/mobile acceptance pass.
- Stage I7A.4E: news create/update now use JSON Route Handlers with HttpOnly-token server adapters; Server Actions are no longer used by the news form. Node 22 isolated medium upload passed desktop/mobile; frontend unit 55/55, lint/build pass.
- Stage I7A.5A: cover transition JSON PATCH coverage added. Isolated backend/frontend build, lint and unit checks pass; desktop transition acceptance is 6/11. External-origin scenarios remain blocked by ERR_CONNECTION_CLOSED/external→owned PATCH timeout; status NOT READY. Canonical report: `D:\Desktop\dev\web\orders\kciasso-backend\отчёт.txt`.
- Stage I7B.2-R: five document regression specs pass individually with trace-on and in full desktop/mobile matrices (5/5 each). Floating popovers use body portals, focus first item on open and return focus to connected anchors on Escape; 70-placement runtime popup scroll/focus checks pass. Frontend unit 55/55, lint and production build pass. Status READY.
- Stage I7C: admin categories, document placement popup, document actions menu and document status control received visual polish. Backend/API unchanged. Category + five document regression specs pass desktop/mobile (6/6 each); frontend unit 55/55, lint/build pass. Status READY.

## Constraints and known risks

- Do not manually edit generated Kubb/Prisma files.
- Do not run destructive checks against production DB or storage.
- Main storage/uploads and user processes must remain untouched.
- Deploy must apply migration `20260716230000_simple_admin_permissions` before the updated backend serves traffic.
- CI Docker build must be rerun after the Prisma generator/import fix; local Docker daemon was unavailable during this verification.
- DEP0190 was not reproduced with `--trace-deprecation` on Node 22 or Node 24; own startup script uses `spawn` without `shell:true`.
- Legacy `UserSectionPermission` can be removed only in a later compatibility-breaking migration after deployment validation.
- Perform manual browser acceptance of Users and News upload when an appropriate runtime is available.

## Current next task

Stage I8.4 acceptance closure is complete: permanent 67-placement fixture, popup geometry/focus coverage, explicit public viewport matrix, browser regressions and full backend/frontend checks are green. No acceptance blocker remains.

## Codebase Memory

- Frontend index refreshed 2026-07-16: 2197 nodes / 4615 edges, `persistence=false`, no repository artifact.
- Backend index refreshed 2026-07-16: 1013 nodes / 2508 edges, `persistence=false`, no repository artifact.
